# Normaliza fin de línea a LF en el commit (garantía real cross-platform,
# independiente del editor). Evita que CRLF de Windows rompa el build en Linux.
* text=auto eol=lf

# Binarios: no tocar.
*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
