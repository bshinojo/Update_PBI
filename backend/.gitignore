# Entorno virtual
.venv
venv
__pycache__/
*.pyc

# Variables de entorno locales
.env

# Persistencia de schedules + historial de corridas (datos locales, no se versiona)
schedules.json
runs.jsonl
*.json.tmp
